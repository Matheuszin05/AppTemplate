<resources>
    <!-- Base app theme -->
    <style name="Base.Theme.AppTemplateProject" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Personalize os atributos do tema -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/black</item>
    </style>

    <!-- Tema da aplicação -->
    <style name="AppTheme" parent="Base.Theme.AppTemplateProject">
        <!-- Personalize os atributos do tema se necessário -->
    </style>
</resources>
